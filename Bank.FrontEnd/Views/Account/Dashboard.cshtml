@model Bank.FrontEnd.ViewModels.ListAndSearchVM
@if (User.Identity.IsAuthenticated)
{
    <div>

                        @Html.Partial("~/Views/Account/DashboardAccount.cshtml", Model.Accounts)

                        @Html.Partial("~/Views/Transaction/DashboardTransactie.cshtml", Model.Transactions)

        </div>















    @*<div class="form-group">
            @Html.LabelFor(x => Model.Selection, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-5">
                @Html.DropDownListFor(x => Model.Selection, new SelectList(Model.Selection, "Value", "Text"), htmlAttributes: new { @class = "form-control", id = "Selecteer" })
                @Html.ValidationMessageFor(x => x.Selection, "", new { @class = "text-danger" })
            </div>
        </div>*@
    @*<table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.SelectedAccount.AccountBalance)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.SelectedAccount.AccountLock)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.SelectedAccount.AccountBalance)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.SelectedAccount.AccountMinimum)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.SelectedAccount.AccountLimiet)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.SelectedAccount.AccountType)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Accounts)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.AccountNumber)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.AccountLock)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.AccountBalance)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.AccountMinimum)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.AccountLimiet)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.AccountType)
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                            <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>*@
}
@if (!User.Identity.IsAuthenticated)
{
    <h2>U dient in te loggen om accounts te kunnen bekijken. </h2>
    <br />
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
}
